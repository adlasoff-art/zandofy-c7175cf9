
L'utilisateur confirme que l'URL Supabase production est bien `https://vpttoqojmiqxgudknyxf.supabase.co` (et non `wgidwyrdnboivfphwete` que j'avais supposé). Je dois donc ajuster le plan v2 en utilisant uniquement cette URL pour les préconnexions et le rewrite sitemap (déjà bon dans `vercel.json`).

Plan inchangé sauf la correction 2 (préconnexions).
