[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
markers =
    integration: marks tests that need a live DB and may be flaky with TestClient (run with -m integration)
